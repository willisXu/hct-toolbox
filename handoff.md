# Handoff — HCT 工具箱

交接說明:做了什麼、驗證到哪、還沒解決什麼。功能面的使用說明在 `README.md`,
這裡只寫**交接需要知道的事**,最新的在最上面。

---

# 2026-08-26:ED 訂單拆解的組合對照表 119 → 255 個套件

## 起因

使用者回報「ED 訂單明細這一功能就無法正常操作」。內建的
`mappings/組合對照表.xlsx` 停在 **119 個套件**,大量 299 開頭的架上組拆不開,
只能原樣保留,拆解功能形同半殘。

## 結果

一天內分五次補到 **255 個套件 / 487 列**,全部已推上 GitHub 並由 Streamlit
Cloud 部署上線(2026-08-26 09:39 UTC 那次 clone 已含最終版)。

| commit | 內容 | 套件數 |
|---|---|---|
| `2bde30a` | 用使用者給的維護檔 `bundles_可編輯.xlsx` 更新 | 119 → 148 |
| `10208ee` | 補 29926085(依使用者提供的 ERP 組合套件畫面截圖) | 148 → 149 |
| `4980d6b` | 補使用者指名的 6 個(改用主檔規格公式推導) | 149 → 155 |
| `3d28ca2` | 2025/2026 可推導的 89 個 | 155 → 244 |
| `a85aff3` | 最後 11 個(AAS030A 歧義確認後) | 244 → **255** |

**既有套件的組成、每套數量、分攤比率從頭到尾一格都沒動** —— 五次都是純新增,
先前跑過的拆解結果不受影響、不必重跑。

## 最關鍵的一件事:對照表的資料來源換了

原本大家都以為對照表只能靠 ERP 匯出的 `bundles.csv`(鼎新 MDxxx 格式)維護。
**那份檔案不可靠**:

- 手上這份只有 **116 個套件**,資料停在 **2026/6/10**
- 客戶代號只涵蓋 8 組,九成集中在 `2111030001`(官網信用卡)與 `2111030004`(官網代收)
- 跳號嚴重:…29926082、29926083 之後直接跳到 29926095
- 電腦上 13 份 `bundles.csv`(HCT/MOMO/Shopline/NS匯入程式各專案)**檔案大小全是
  96,402 bytes** —— 同一份舊檔在各專案間複製流傳,大家其實都在用同一份不完整的資料

真正好用的來源是 **NS 品項主檔 `items.正式區.xlsx` 的「規格」欄,它本身就是組合公式**:

```
29926085 玻尿酸保濕精華液 團購組   規格  HHS030A*4+HHS015A
29925164 玻尿酸保濕精華5入囤貨組   規格  HHS030A*5
29925048 高效補水修護組            規格  HHT150A+DCS015A+HHL050A
```

規格代碼再用同一份主檔反查單品品號,拆法就還原出來了。**29926085 用這個方法
推出來的結果,與使用者提供的 ERP 畫面截圖完全一致**,這是方法可信的第一個證據。

## 推導規則與工具

已把推導邏輯收進 **`tools/derive_bundle_map.py`**(不是一次性腳本,可重跑):

```bash
# 先看會新增什麼(不寫檔)
python tools/derive_bundle_map.py <品項主檔.xlsx> <bundles.csv>
# 確認後才寫回 mappings/組合對照表.xlsx
python tools/derive_bundle_map.py <品項主檔.xlsx> <bundles.csv> --apply
```

收錄條件**三個都要成立**,任一不成立就跳過留給人工:

1. 「規格」是看得懂的組合公式(`A+B*2` 這種)
2. 公式裡每個規格代碼在主檔**只對到一個單品**(非 29 開頭的品號)
3. 該單品在 `bundles.csv` 查得到標準售價(算分攤比率要用)

分攤比率 = **標準售價 × 數量**加權,取兩位小數、尾差併入比率最大者(加總=1)。

預設只處理 `29925`/`29926` 前綴 —— **299 後面兩碼是年份**(29925xxx = 2025 年開的)。
主檔裡 2014~2024 的舊架上組還有兩百多個推得出來,但多半早就不賣了,不要無故灌進去
(真要處理用 `--prefix 299`)。

## 準確度回測

拿當時現行的 155 個套件用同一套規則重算:**推導得出結果的 93 個裡 91 個組成與
數量完全相同(97.8%)**。兩個不同的都是同一個規格代碼造成的,見下一節。

補完後跑過的驗證(每次都跑):
- 既有套件 signature 逐一比對,確認沒被動到
- 所有套件比率加總 = 1、無空品號
- 端到端實跑 `bundle_split.split()`,確認**金額守恆**、未對應 0 筆
- `python tests/test_core.py` 65/65 通過

## ⚠ AAS030A:唯一做過主觀判斷的地方

主檔裡「超逆齡肌因再生精華30ML」有多支品號,規格帶年月的是舊版/改版品:

| 品號 | 規格 | 備註 |
|---|---|---|
| 102041170007 | `AAS030A(2404)` | 2024 版 |
| 102041170012 | `AAS030A(2507)` | 2025/07 版 |
| **102041170013** | `AAS030A` | 不帶年月 = 現行品(序號也最大) |

**所有 299 組合的公式寫的都是不帶年月的 `AAS030A`**,全主檔沒有任何一個寫
`AAS030A(2507)`。而現行表裡「表上規格 = `AAS030A`」的 6 個套件**全部**用
102041170013,零例外;用 …012 的那 2 個(29925208、29926048)表上規格記的是
`AAS030A(2507)`,本來就是不同東西 —— 回測那兩個「不一致」是**公式沒跟上換版**,
不是對照規則衝突。同名的「超逆齡肌因再生精華30ML 1+1組」在較舊的 29926048 用
…012、較新的 29926082 用 …013,看得出是換版。

依此把最後 11 個(29925017 29925038 29925045 29925116 29925174 29925221
29925238 29926017 29926027 29926076 29926088)一律用 **102041170013**。

> **殘餘風險**:萬一 ERP 那邊某幾個其實用的是 (2507) 版,就會拆出錯的品號。
> 對帳發現哪一個不對,單獨改那一列即可,不必整批重來。

## 其他已知但刻意沒動的事

- **29923039 兩邊講法不同**:ERP 匯出檔裡這個套件有兩版 —— 生效 20230626 是
  「玻尿酸保濕潔顏慕斯150ML ×2」、生效 **20231016** 是「維他命C瞬效亮白膠囊面膜4入 ×1」。
  線上一直用的是 20230626 那版,維護檔也是。ERP 上比較新的是面膜那版。
  **維持現狀沒動**,要改需業務確認。
- **29926117 / 29926118 / 29926119 同一個單品有兩列**(每套 10 + 2、比率 0.83/0.17),
  是 ERP 買 10 送 2 的定義方式,照原樣保留,拆解後會出兩列合計 12 個。不是重複列。
- **`bundles_可編輯.xlsx`(使用者的維護檔)沒有被同步**。它在使用者的 Downloads,
  不在版控裡。**下次若拿它來更新對照表,會把今天補的 107 個洗掉** —— 要嘛先把
  對照表的內容倒回那份維護檔,要嘛改成一律用 `tools/derive_bundle_map.py` 維護。

## 還沒解決:2025/2026 還有 143 個收不進來

主檔 29925/29926 共 340 個,已收錄 197、**未收錄 143**:

| 卡住原因 | 數量 | 說明 |
|---|---|---|
| 規格代碼對不到唯一單品 | 65 | 像 `HHS015A` 同時對到 5 個品號,得看 ERP 才知道用哪支 |
| 缺標準售價 | 63 | `bundles.csv` 只到 2026/6/10,涵蓋不到這些單品 |
| 非組合公式 | 15 | 主檔規格欄不是 `A+B` 格式 |

**根治方法只有一個:請使用者重新匯一份完整的 `bundles.csv` —— 不限客戶代號、
不限日期。** 有 ERP 的組合明細就不必用推導的,歧義與售價兩個問題會同時消失。
這件事已經跟使用者提過三次,尚未拿到檔案。

拿到新檔之後的步驟:
1. 先跑 `python tools/derive_bundle_map.py <主檔> <新bundles.csv>`(不帶 --apply)看差異
2. 直接用 ERP 明細的部分優先於推導 —— `core/bundle_split.py` 的 `load_bundle_map()`
   本來就吃得下 MDxxx 原始格式(`_load_erp`,同一套件多版取生效日期最新)
3. 一併回頭核對今天推導出來的 107 個,特別是 AAS030A 那 11 個

## 使用者若回報「線上還是舊的」

1. 重新整理頁面
2. 仍舊 → Streamlit Cloud 後台按 **Reboot app**(休眠喚醒時偶爾晚一步)
3. 急著用 → ED 訂單拆解分頁的「組合對照表」展開區可以**直接上傳對照表覆蓋**,
   當次立即生效,不必等部署

確認版本的方法:展開區按「⬇️ 下載目前內建的對照表」,最新版是 **487 資料列**。

---
